# Docker Compose file format version
version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: base-source-code
    container_name: base-source-code
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
    restart: unless-stopped
